package main;

/**
 * The ISettingsController provider the interface for the SettingsController
 */
public interface ISettingsController {

	/** This method gets and handles the admin selection.
	 *	The admin is allowed to update the base price or the different multipliers
	 */
	public void run();
}
